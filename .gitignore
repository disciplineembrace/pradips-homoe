skills/
node_modules/
tool-results/

.next/
worklog.md
*.bak-*
data/sources/
data/remedies.json.bak-*
data/remedies_backup_*

# Derived indexes — rebuilt at runtime from remedies.json, not needed in git
data/remedies-by-id.json
data/remedies-index.json

# Local backups (preserved on disk for recovery, not in git)
data/remedies.json.backup-*
data/*_backup_*.json
data/*_pre_*.json
data/kent_backup_*.json

# APK files — served via GitHub Releases, not bundled in Vercel deploy
download/*.apk
download/*.aab

# Vercel CLI local config
.vercel
.vercel/

# Dev server log
dev.log
